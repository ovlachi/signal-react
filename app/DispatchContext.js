import { createContext } from "react";

const DispatchContext = createContext();

export default DispatchContext;
// This code creates a context for the application, which can be used to share state and functions across components without having to pass props down manually at every level.
